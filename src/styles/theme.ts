// src/styles/theme.ts
export const theme = {
  fontFamily: 'var(--font-base)',
  fontWeight: 400,
  colors: {
    text: 'var(--color-text)',
    background: 'var(--color-background)',
  },
  backgroundImage: 'var(--background-pattern)',
} as const
